global:
  scrape_interval: 15s
  evaluation_interval: 30s

rule_files:
  - alerts.yml

scrape_configs:
  - job_name: 'hefa-api'
    static_configs:
      - targets:
          - 'localhost:3000'   # your API host:port
    metrics_path: /metrics
    scheme: http
